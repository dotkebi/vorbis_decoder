name: vorbis_decoder_ffi_compare
description: Development-only pub.dev FFI comparison harness for vorbis_decoder.
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  ffi: ^2.1.3
  vorbis_decoder:
    path: ../..
  vorbis_decoder_ffi: ^0.2.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
